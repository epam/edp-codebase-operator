apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-application-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: business-application-operator
  template:
    metadata:
      labels:
        name: business-application-operator
    spec:
      serviceAccountName: business-application-operator
      containers:
        - name: business-application-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - business-application-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "business-application-operator"
